name: pyhydrogeophysx
channels:
  - gimli
  - conda-forge
  - defaults
dependencies:
  - python>=3.10,<3.13
  - numpy>=2.2,<2.3
  - scipy>=1.8,<3.0
  - matplotlib>=3.5,<4.0
  - pandas>=1.3
  - plotly>=5.18,<7.0
  - streamlit>=1.35,<2.0
  - joblib>=1.2
  - tqdm>=4.62
  - suitesparse
  # Geophysics engines (recommended via conda for binary deps)
  - pygimli>=1.5.5,<2.0
  - pip
  - pip:
      - simpeg>=0.24,<1.0
      - flopy>=3.5,<4.0
      - pftools>=1.3
      - streamlit-plotly-events>=0.0.6
      - weasyprint>=60.0
      - pyhydrogeophysx

# Core scientific computing
numpy>=1.19
scipy>=1.5
matplotlib>=3.2
tqdm>=4.0
pyarrow>=10.0

# Geophysics libraries
# Note: pygimli may require conda for best compatibility:
#   conda install -c gimli pygimli
pygimli>=1.5
simpeg>=0.20
resipy>=3.4.0

# LLM APIs (at least one required for agent functionality)
openai>=1.0
anthropic>=0.18
google-generativeai>=0.3

# Web app
streamlit>=1.28
plotly>=5.0
streamlit-plotly-events>=0.0.6

# PDF generation (optional, for report export)
# Note: weasyprint requires system dependencies (cairo, pango)
# On Streamlit Cloud, add to packages.txt: libcairo2-dev libpango1.0-dev
weasyprint>=60.0

# Utilities
requests>=2.28
joblib>=1.3

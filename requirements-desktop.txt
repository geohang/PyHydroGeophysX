# Desktop (Qt) studio dependencies for PyHydroGeophysX.
# Install into the same Python environment that runs the package / Streamlit app:
#     pip install -r requirements-desktop.txt
#
# These power PyHydroGeophysX.qt_apps (the professional desktop studio).

PySide6>=6.5
pyqtgraph>=0.13
qtawesome>=1.2
numpy>=1.21
pandas>=1.3.0

# The 3D viewers (Mesh 3D, velocity volume, clip-plane preview). Left commented
# out because vtk is a large binary wheel; without them those panels show an
# install message and the rest of the studio is unaffected. Equivalent to
#     pip install "pyhydrogeophysx[desktop-3d]"
#
# Before installing, check which tool manages this environment's packages:
#     conda list numpy
# `pypi` means pip owns them, even in a conda-created environment, so use the
# line above. A conda channel means conda owns them, so use
#     conda install -c conda-forge pyvista pyvistaqt
# Taking the other route leaves two VTK builds on the path.
# pyvista>=0.40
# pyvistaqt>=0.10
# vtk>=9.2

# Gridding and interpolation in the gravity / EM modules:
# scipy>=1.8
